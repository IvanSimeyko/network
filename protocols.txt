ARP RARP - трансляция айпи адреса в макадрес и наоборот (adress resolution protocol)
DHCP - dinamic host configurations protocol. Точка доступа выдает ip в сети
ТСР -  протокол контроля передачи данных
UDP (user datagramm protocol) - протокол полуения данных без проверки данных  
DNS domane name server
RIP (road internet protocol) - рассказывает о напралении двиения трафика маршрутизатором (указывает в каком направлении находится определенная сеть и количество хостов (маршрутов) до нее)
O(A)SPF - тоже, только каждый маршрутизатор строит свою карту сети и знает куда отправлять пакеты
PGP - 
HTTP - протокол прикладного уровня.


